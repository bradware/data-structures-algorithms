public class QuickSort {

	/**
	 * Sort the array using quick sort, this must be:
	 * 
	 * in-place (no temporary arrays)
	 * O(n log n)
	 * 
	 * We will make sure you do not use any extra memory for this 
	 * method. If you use any extra memory(such as creating a new array)
	 * you will receive no credit for this problem. You may, however, create any
	 * variables you need. The auto-grader will be checking to make sure you are
	 * sorting the array passed in without creating any additional arrays.
	 * 
	 * @param array
	 * 
	 */
	public static <T extends Comparable<? super T>> void quickSort(T[] array) {
		// not ever necessary to write this myself lol
	}
	
}
